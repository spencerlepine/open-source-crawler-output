## Detected Typos Diff
```diff
--- ./loaders/getArticle.js	original
+++ ./loaders/getArticle.js	fixed
@@ -49 +49 @@
-            var serie;
+            var series;
@@ -52,3 +52,3 @@
-                    serie = series[Math.floor(Math.random() * series.length)];
-                } while (out.indexOf(serie) > -1);
-                out[i] = serie;
+                    series = series[Math.floor(Math.random() * series.length)];
+                } while (out.indexOf(series) > -1);
+                out[i] = series;
